# ds-product-template — 本 template 預設不需 .env
#
# Access control 預設用 Netlify Basic Password(共用密碼,在 Netlify dashboard 手動設,
# 不靠任何 env var)。`npm run setup:netlify` 跑 CLI install + login + site 建 + 連 repo
# 後,印出 dashboard URL,跟著點 2 個 radio button 輸密碼即可(30 秒)。
#
# → 詳見 CLAUDE.md 「🔒 Access control — Basic Password」段。
#
# 目前無實質 env var 需要;若未來新增,再 copy 本檔成 .env(被 .gitignore 忽略)。
